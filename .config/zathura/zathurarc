# the command on the righ can be executed manually by typing column `:`

# by default zathura monitor the open file and autorelods it, if it changes
map r reload
map R rotate
map K zoom in
map J zoom out
# recolor is the night mode
map i recolor
map p print
